<?xml version="1.0" encoding="utf-8"?>
<!--
    HTTPS forcé par défaut (cleartext refusé) : couvre tout l'Internet public,
    là où le MITM est un vrai risque. En externe l'app passe par Pangolin/Traefik
    en HTTPS, géré par le base-config (ancre de confiance système).

    Le HTTP en clair n'est autorisé que pour les hôtes LAN listés ci-dessous, car
    le serveur Excerpta peut tourner en HTTP sur le réseau local.
    network-security-config n'accepte PAS de plage CIDR : il faut énumérer les
    IP/domaines. Si tu accèdes au serveur via une IP LAN absente de cette liste,
    la connexion HTTP sera refusée — ajoute simplement un <domain> ici.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Loopback / émulateur Android -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>

        <!-- TLD interne résolu par Pi-hole (*.lan) -->
        <domain includeSubdomains="true">lan</domain>

        <!-- Serveurs LAN connus (subnets 192.168.1.x / 192.168.2.x) -->
        <domain includeSubdomains="false">192.168.1.18</domain>
        <domain includeSubdomains="false">192.168.2.10</domain>
        <domain includeSubdomains="false">192.168.2.11</domain>
    </domain-config>
</network-security-config>
